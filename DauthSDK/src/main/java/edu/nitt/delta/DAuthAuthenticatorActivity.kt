package edu.nitt.delta

import android.accounts.Account
import android.accounts.AccountAuthenticatorActivity
import android.accounts.AccountAuthenticatorResponse
import android.accounts.AccountManager
import android.annotation.SuppressLint
import android.content.Intent
import android.content.SharedPreferences
import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import android.util.Log

class DAuthAuthenticatorActivity : AccountAuthenticatorActivity() {
//    private val TAG = "authactivity"
//    @SuppressLint("CommitPrefEdits")
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
//        setContentView(R.layout.activity_dauth_authenticator)
//        val accountManager = AccountManager.get(baseContext)
//        val account =
//            Account("pranav12345", "auth.delta.nitt.edu")
//        val added: Boolean = accountManager.addAccountExplicitly(account,"Pranav1811" , Bundle())
//        Log.d(TAG, "addAccount: $added")
//        val intent = Intent()
//        intent.putExtra(AccountManager.KEY_ACCOUNT_NAME, "pranav1234")
//        intent.putExtra(AccountManager.KEY_ACCOUNT_TYPE, android.R.attr.accountType)
//        setAccountAuthenticatorResult(intent.extras)
//        setResult(RESULT_OK, intent)
//        finish()
    }
}